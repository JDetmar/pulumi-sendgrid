name: sendgrid
runtime: yaml

resources:
  myRandomResource:
    type: sendgrid:Random
    properties:
      length: 24
  myRandomComponent:
    type: sendgrid:RandomComponent
    properties:
      length: 24

outputs:
  output:
    value: ${myRandomResource.result}
